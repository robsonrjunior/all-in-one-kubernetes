apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: all-in-one

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - storage/postgres.yaml
  - storage/redis.yaml
